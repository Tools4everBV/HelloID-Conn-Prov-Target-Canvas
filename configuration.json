[
    {
        "key": "ClientId",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "ClientId",
            "description": "The ClientId to connect to the API",
            "required": true
        }
    },
    {
        "key": "ClientSecret",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "type": "password",
            "description": "The ClientSecret to connect to the API",
            "label": "ClientSecret",
            "required": true
        }
    },
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "BaseUrl",
            "description": "The URL to the API.",
            "required": true
        }
    },
    {
        "key": "RedirectUri",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "RedirectUri",
            "description": "The redirect uri used in the initial request",
            "required": true
        }
    },
    {
        "key": "Code",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "Code",
            "description": "This code will be provided by Canvas",
            "required": true
        }
    },
    {
        "key": "AccountId",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "Code",
            "description": "The AccountId under which the user objects are created",
            "required": true
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
            "label": "Toggle debug logging",
            "description": "When toggled, debug logging will be displayed",
            "required": false
        }
    }
]
